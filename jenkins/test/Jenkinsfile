#!groovy

def sharedLib = new SharedLib()

node {
    stage('Prepare') {
        sharedLib.prepare()
    }
    stage('Build') {
        sharedLib.purge()
        sharedLib.build()
    }
    stage('Integration test') {
        sharedLib.runITs()
    }
}